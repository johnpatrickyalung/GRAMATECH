services:
  - type: web
    name: gramatech-api
    runtime: node
    buildCommand: npm install --legacy-peer-deps
    startCommand: node server/index.js
    envVars:
      - key: NODE_ENV
        value: production
